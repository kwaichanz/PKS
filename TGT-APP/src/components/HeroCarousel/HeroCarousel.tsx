"use client";

import React from "react";
import { items } from "../../../mocks/HeroCarouselItems.json";

export const HeroCorousel = () => {
  const { responsive } = items;
  return (
    <div className=""></div>
  );
};
